{
  "name": "@annasetu/polygon-contracts",
  "version": "1.0.0",
  "description": "AnnaSetu Solidity smart contracts for Polygon public transparency ledger",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.19.4",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}
